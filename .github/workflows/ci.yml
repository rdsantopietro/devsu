name: Docker Image CI

on:
  push:
    branches: [ "ci" ]
  pull_request:
    branches: [ "ci" ]

jobs:

  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'

    - name: Build the Docker image in the movimiento directory
      run: |
        cd movimiento
        mvn clean package
        docker build . --file ./Dockerfile --tag rdsantopietro/movimiento
  
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
         username: ${{ vars.DOCKER_USER }}
         password: ${{ secrets.DOCKER_PASSWORD }}
         
    - name: Build and push Docker image
      run: docker push rdsantopietro/movimiento:latest
  
  
  pull:
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH key
        uses:  uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key:: ${{ secrets.SSH_EC2_AWS_PRIVATE_KEY }}

      - name: SSH into the host and download Docker image
        run: |
          ssh -i /path/to/private/key.pem ubuntu@ec2-107-21-189-154.compute-1.amazonaws.com "docker pull movimiento"
    
         
      
          
       
