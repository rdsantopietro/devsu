name: Docker Image CI

on:
  push:
    branches: [ "ci" ]
  pull_request:
    branches: [ "ci" ]

jobs:

  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'

    - name: Build the Docker image in the movimiento directory
      run: |
        docker login -u rdsantopietro@gmail.com --password-stdin ${{ secrets.DOCKER_PASSWORD }}
        docker push rdsantopietro/movimiento:latest
      #  cd movimiento
      # mvn clean package
      #  docker build . --file ./Dockerfile --tag rdsantopietro/movimiento:latest
  
  push:
     runs-on: ubuntu-latest
     needs: build 
     steps:
     - name: Push image to Docker Hub
       run: |
       #  echo "${{ secrets.DOCKER_PASSWORD }}" | docker login --username rdsantopietro@gmail.com --password-stdin
        # docker push rdsantopietro/movimiento:latest
